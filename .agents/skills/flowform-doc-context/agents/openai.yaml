interface:
  display_name: "FlowForm Documentation Context"
  short_description: "Load focused FlowForm documentation before changing the system"
  default_prompt: "Use $flowform-doc-context to plan or implement this task against verified FlowForm repository evidence."
dependencies:
  tools:
    - type: "mcp"
      value: "flowform-docs"
      description: "Retrieves focused FlowForm documentation context and impact reports."
      transport: "stdio"
