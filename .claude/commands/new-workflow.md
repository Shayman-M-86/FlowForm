Scaffold a new Claude workflow directory.

```bash
bash .claude/workflows/scripts/new-workflow.sh
```

Run with the Bash tool directly. The script is interactive — it will prompt
for workflow name, description, number of passes, and pass names.

After it runs, follow the printed next steps to populate source docs and
fill in the target spec/prompt files before starting a session.
