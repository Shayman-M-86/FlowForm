# Pass Report Template

Write completed reports to `working/pass-reports/<N>-<slug>.md`.
Local plan stays in chat only — never written to disk.

---

## Pass report

Pass: <number> — <name>

Changed files:

* ...

Behavior implemented:

* ...

Tests run:

* `<exact command>` — <N> passed

Failures or skipped validation:

* ...

Policy change during pass:

* (note any source doc edits made to resolve disagreements, or "none")

Trace notes:

* entry points touched:
* methods touched:
* side effects changed:
* tests that now describe behavior:

Remaining risks:

* ...

Next recommended pass:

* ...
