# Validation Ladder

Start with focused `-k` tests. Climb the ladder as risk rises.
Skipped validation is not green — record it in the pass report.

**Before choosing your test strategy**, add a todo item:
"Read `.claude/workflows/session-encryption/source/validation-ladder-full.md` for levels and selectors."
Do NOT skip this — the stub above is a summary, not the full specification.
