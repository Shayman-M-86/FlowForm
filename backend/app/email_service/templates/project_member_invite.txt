FlowForm project invitation

Hi {{ recipient_name or "there" }},

{% if inviter_name -%}
{{ inviter_name }} has invited you to join {{ project_name }}.
{%- else -%}
You have been invited to join {{ project_name }}.
{%- endif %}

Accept invitation:
{{ invite_url }}

{% if expires_at -%}
This invitation expires on {{ expires_at.strftime("%d %b %Y at %I:%M %p") }}.
{%- endif %}

If you were not expecting this email, you can safely ignore it.