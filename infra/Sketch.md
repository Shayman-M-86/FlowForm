
ENV:
  DATABASE:

    DATABASE_INIT_TARGET: core | response

      CORE:
          INIT:
              POSTGRES_USER <- DATABASE_CORE_INIT_USER
              POSTGRES_DB <- DATABASE_CORE_INIT_DB
              POSTGRES_PASSWORD_FILE <- /run/secrets/DATABASE_CORE_INIT_PASSWORD
          MAIN: 
              DATABASE_CORE_HOST
              DATABASE_CORE_PORT
              DATABASE_CORE_NAME
              DATABASE_CORE_APP_USER
              DATABASE_CORE_APP_PASSWORD_FILE <- /run/secrets/DATABASE_CORE_APP_PASSWORD
              DATABASE_CORE_SCHEMA_FILE
          secrets:
              DATABASE_CORE_INIT_PASSWORD:
                file: ./secrets/DATABASE_CORE_INIT_PASSWORD.secret.txt
              DATABASE_CORE_APP_PASSWORD:
                file: ./secrets/DATABASE_CORE_APP_PASSWORD.secret.txt

      RESPONSE:
          INIT:
              POSTGRES_USER <- DATABASE_RESPONSE_INIT_USER
              POSTGRES_DB <- DATABASE_RESPONSE_INIT_DB
              POSTGRES_PASSWORD_FILE <- /run/secrets/DATABASE_RESPONSE_INIT_PASSWORD
          MAIN:
              DATABASE_RESPONSE_HOST
              DATABASE_RESPONSE_PORT
              DATABASE_RESPONSE_NAME
              DATABASE_RESPONSE_APP_USER
              DATABASE_RESPONSE_APP_PASSWORD_FILE <- /run/secrets/DATABASE_RESPONSE_APP_PASSWORD
              DATABASE_RESPONSE_SCHEMA_FILE
          secrets:
              DATABASE_RESPONSE_INIT_PASSWORD:
                file: ./secrets/DATABASE_RESPONSE_INIT_PASSWORD.<dev|test|prod>.secret.txt
              DATABASE_RESPONSE_APP_PASSWORD:
                file: ./secrets/DATABASE_RESPONSE_APP_PASSWORD.<dev|test|prod>.secret.txt

  FLOWFORM:
    ENV:
      FLOWFORM_ENV
    APP:
      FLOWFORM_APP_DEBUG,
      FLOWFORM_APP_SECRET_KEY_FILE

    AUTH0:
      FLOWFORM_AUTH0_DOMAIN,
      FLOWFORM_AUTH0_AUDIENCE

    LOGGING:
      FLOWFORM_LOGGING_LEVEL,
      FLOWFORM_LOGGING_LOG_JSON,
      FLOWFORM_LOGGING_LOG_FILE,
      FLOWFORM_LOGGING_REQUESTS,
      FLOWFORM_LOGGING_DURATION

    SERVER:
      FLOWFORM_SERVER_HOST,
      FLOWFORM_SERVER_PORT

    RATE_LIMIT:
      FLOWFORM_RATE_LIMIT_ENABLED,
      FLOWFORM_RATE_LIMIT_MAX_REQUESTS,
      FLOWFORM_RATE_LIMIT_WINDOW_SECONDS
