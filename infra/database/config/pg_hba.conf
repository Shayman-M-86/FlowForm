local   all             postgres                                peer
local   all             all                                     scram-sha-256
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# Docker network (any bridge subnet)
host    all             all             172.30.0.0/16           scram-sha-256
host    all             all             172.50.0.0/16           scram-sha-256

# Rehearsal app VM; VM 240's nftables policy independently restricts :5432.
host    all             all             10.10.10.20/32          scram-sha-256
