# CI override: use the pre-built image produced by docker/build-push-action
# instead of building from the Dockerfile.
#
# Usage (via GitHub Actions):
#   docker compose -f docker-compose.test.yml -f docker-compose.ci.yml up -d

services:
  backend-test:
    image: flowform-backend-test:latest
    build: !reset null
