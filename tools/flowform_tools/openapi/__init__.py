"""FlowForm OpenAPI tooling.

Exposes the live backend OpenAPI specification — endpoints, schemas, and
documented errors — to AI agents over MCP, authenticating through Auth0.

Unlike the rest of ``flowform_tools`` this subpackage requires third-party
dependencies; install them with ``uv sync --extra openapi``.
"""
