[project]
name = "flowform-mcp"
version = "0.1.0"
description = "FlowForm dev MCP server — exposes the backend OpenAPI spec to MCP clients"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=48.0.0",
    "fastmcp>=3.3.1",
    "httpx>=0.27.0",
]
